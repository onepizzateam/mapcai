# Upstash Redis (provision via Vercel Marketplace — NOT Vercel KV, which is deprecated)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Upstash QStash — scheduled writer invocation + webhook signature verification
QSTASH_TOKEN=
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# Shared secrets — gate the writer webhook and the non-prod seed route
WRITER_SECRET=
SEED_SECRET=
